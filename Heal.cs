using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Heal : MonoBehaviour
{/*
    public float healAmount = 10f;
    // Start is called before the first frame update
    private void OnTriggerEnter(Collider autre)
    {
        print(autre.gameObject.name + " viens de me rentrer dedans");
        autre.gameObject.SendMessage("Healing", healAmount);

        if(autre.gameObject.TryGetComponent<PlayerHP>(out var playerHpScript))
        {
            print("on a trouvé le composant " + playerHpScript.name);
            if (playerHpScript.Healing(healAmount))
            {
                print("Soins appliqués !");
                Destroy(gameObject);
            }
            else
            {
                print("Pas de soins appliqués !");
            }
        }
        else
        {
            print("on a pas trouvé le composant, donc ce n'est pas le player");
        }
    }*/
}
